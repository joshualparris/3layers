{
  "framework": "vite",
  "installCommand": "cd Epic-Quest-Saga && corepack enable && corepack prepare pnpm@10.29.3 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd Epic-Quest-Saga && pnpm --filter @workspace/continuity-app build",
  "outputDirectory": "Epic-Quest-Saga/artifacts/continuity-app/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
